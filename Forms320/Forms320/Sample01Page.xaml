<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:system="clr-namespace:System;assembly=netstandard"
             x:Class="Forms320.Sample01Page">

    <StackLayout Orientation="Vertical">
        <Label TextColor="Gray" HorizontalOptions="Center" VerticalOptions="Center">
            <Label.FormattedText>
                <FormattedString>
                    <Span Text="{Binding FirstName}" FontSize="Large" FontAttributes="Bold" TextColor="Red"/>
                    <Span Text=" " FontSize="Large"/>
                    <Span Text="{Binding LastName}" FontSize="Large" FontAttributes="Bold" TextColor="Blue"/>
                    <Span Text="{x:Static system:Environment.NewLine}" FontSize="Large"/>
                    <Span Text="{Binding Twitter}" FontAttributes="Italic" TextColor="Gray"
                          FontSize="Medium"/>
                </FormattedString>
            </Label.FormattedText>
        </Label>

        <Entry />
    </StackLayout>

</ContentPage>
